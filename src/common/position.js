import {
        Dimensions
} from 'react-native';

const { width, height } = Dimensions.get('window');

export default {
        Header: {
                heightHeader: 400,
                paddingLeftRightSearch: 10,
                heightSearch: 40
        },

};

